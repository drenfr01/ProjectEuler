object Problem45 {

  def main(args : Array[String]) : Unit = {
    
    var triangleNumbers : List[Int] = List()
    var pentagonalNumbers : List[Int] = List()
    var hexagonalNumbers : List[Int] = List()

    
    for(n <- 143 to 10000000)
    {
      
    }
  }
  
  def calculateTriangleNumber(n : Int) : Int = {
    return n * (n+1) / 2
  }
  
  def calculatePentagonalNumber(n : Int) : Int = {   
    return n * (n+1) / 2
  }
   
  def calculateHexagonalNumber(n : Int) : Int = {
    return n * (n+1) / 2
  }

}

